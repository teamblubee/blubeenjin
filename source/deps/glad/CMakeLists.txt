SET(CMAKE_MACOSX_RPATH 1)

SET(HEADER_FILES "${CMAKE_CURRENT_SOURCE_DIR}/headers/glad.h")
SET(HEADER_FILES "${CMAKE_CURRENT_SOURCE_DIR}/headers/khrplatform.h")
SET(SRC_FILES "${CMAKE_CURRENT_SOURCE_DIR}/glad.c")

SET(INCLUDE_DIR "${CMAKE_CURRENT_SOURCE_DIR}/headers")


ADD_LIBRARY(glad SHARED ${SRC_FILES} ${HEADER_FILES})
TARGET_INCLUDE_DIRECTORIES(glad PUBLIC ${INCLUDE_DIR})
